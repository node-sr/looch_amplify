#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createUser_statusInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createUser_statusInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO User_status $colStr VALUES $valStr", "SELECT * FROM User_status WHERE id=$ctx.args.createUser_statusInput.id"]
}