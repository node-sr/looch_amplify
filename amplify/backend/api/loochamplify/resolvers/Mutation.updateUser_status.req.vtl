#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateUser_statusInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateUser_statusInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE User_status SET $update WHERE id=$ctx.args.updateUser_statusInput.id", "SELECT * FROM User_status WHERE id=$ctx.args.updateUser_statusInput.id"]
}